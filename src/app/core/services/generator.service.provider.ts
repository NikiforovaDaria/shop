// import { GeneratorService } from './generator.service';
// import { RandomNumberService } from './random-number.service';

// //  generatorServiceProvider = (length: number): any => {
// //   return new GeneratorService(length);
// // }

// //  let generatorServiceProvider = {
// //   provide: GeneratorService,
// //   useFactory: generatorServiceFactory
// // }

// export let generatorServiceFactory = (randomNumberService: RandomNumberService) => {
//   return new GeneratorService(randomNumberService.generate());
// }
