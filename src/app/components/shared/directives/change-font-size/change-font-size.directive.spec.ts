import { ChangeFontSizeDirective } from './change-font-size.directive';

describe('ChangeFontSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeFontSizeDirective();
    expect(directive).toBeTruthy();
  });
});
