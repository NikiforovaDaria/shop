import { ChangeHoverStyleDirective } from './change-hover-style.directive';

describe('ChangeHoverStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeHoverStyleDirective();
    expect(directive).toBeTruthy();
  });
});
